import SwiftUI
import MelogoldCore

/// «Тренды»: «В тренде», «Настроения и жанры» (REWRITE §3.3) — срез 3.
struct TrendsView: View {
    var body: some View {
        SectionPlaceholder(section: .trends)
    }
}
