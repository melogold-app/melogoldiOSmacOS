import SwiftUI
import MelogoldCore

/// «Библиотека» — хаб коллекций (REWRITE §3.2) — срез 4.
struct LibraryView: View {
    var body: some View {
        SectionPlaceholder(section: .library)
    }
}
