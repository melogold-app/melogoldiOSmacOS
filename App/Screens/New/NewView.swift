import SwiftUI
import MelogoldCore

/// «Новое»: новые альбомы и синглы, «Для вас», похожие (REWRITE §3.4) — срез 3.
struct NewView: View {
    var body: some View {
        SectionPlaceholder(section: .new)
    }
}
