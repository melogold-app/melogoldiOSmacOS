# Ход работы: Melogold для Apple

Что сделано по срезам `docs/PROMPT.md` §7, как проверено и что осталось. Следующая сессия продолжает отсюда.

## Как проверять

- `scripts/generate-project.sh` — проект из `project.yml` (XcodeGen собирается из `BuildTools/`).
- `swift test --package-path Packages/MelogoldKit` — правила и общие векторы (`spec/`).
- Сборки (одна `xcodebuild` за раз — Mac общий):
  - `xcodebuild -scheme Melogold -destination 'generic/platform=iOS Simulator' build` — iPhone, iPad и встроенные часы;
  - `-destination 'platform=macOS'`, `'generic/platform=visionOS Simulator'`; схема `Melogold Watch` — `'generic/platform=watchOS Simulator'`.
- UI-тесты iPhone: `TEST_RUNNER_MELOGOLD_SHOTS_DIR=<папка> xcodebuild test -scheme Melogold -destination 'platform=iOS Simulator,id=<UDID>'` — нажатия и снимки экрана.
- Симуляторы: iPhone 18 Pro `0FAF8790-…`, iPad Pro 13 `AB782A98-…`, Apple Watch Series 12 46 мм `4BC3A3E8-…`, Apple Vision Pro `DC9E9E35-…`.
  Параметры запуска: `-shell.lastTab <раздел>`, `-AppleLanguages "(ru)"`, `-MelogoldOpenURL <ссылка>`.
- Mac: пока экран заблокирован, системный снимок чёрный; отладочная сборка снимает своё окно сама
  (`-MelogoldSnapshotPath <png> -MelogoldSnapshotDelay 12 -MelogoldSnapshotQuit YES`), но боковую панель на стекле
  в такой снимок система не отдаёт. `-MelogoldMute YES` — без звука (Mac общий).
- **Сеть симуляторов:** когда на Mac включён VPN «Happ», у системного резолвера нет серверов и симуляторы не
  разрешают имена («Сервер с указанным именем хоста не удалось найти»), а процессы самого Mac — разрешают.
  Обход только для отладки: CONNECT-прокси на Mac (`scratchpad/tools/connectproxy.py 8899`) и параметр запуска
  `-MelogoldDebugProxy 127.0.0.1:8899` (UI-тестам — `TEST_RUNNER_MELOGOLD_PROXY=127.0.0.1:8899`).
- Отладочные параметры: `-MelogoldOpen album:<id>|artist:<id>|playlist:<id>|moods|releases` (детальный экран),
  `-MelogoldOpenLink "<ссылка или текст>"` (как вставка в Поиске), `-MelogoldMute YES` (без звука — всегда при проверке
  воспроизведения: симулятор играет через динамики Mac, а рядом спят; после проверки — `xcrun simctl terminate`),
  `-MelogoldSearch "<запрос>"`, `-MelogoldSearchScope music|youtube`, `-MelogoldPlayFirst YES`
  (iPhone, iPad, Mac, часы), `-MelogoldBenchmark "<запрос>" -MelogoldBenchmarkClearCache YES -MelogoldBenchmarkMute YES
  -MelogoldBenchmarkQuit YES` — замер «от нажатия до звука» по 10 трекам в журнал.
- Живые тесты пакета: `MELOGOLD_LIVE=1 swift test --filter "LiveYouTubeTests|LivePlaybackTests"`.
- Сборки и `swift test` — через `/usr/bin/lockf -k <scratchpad>/locks/xcode.lock …`: Mac общий с основной сессией.

## Срезы

| Срез | Состояние | Коммит |
|---|---|---|
| 0. Бриф и задания под самостоятельные часы | сделано | `093a014` |
| 1. Каркас | сделано | `c992022` |
| 2. Воспроизведение (+ задание 0003) | сделано; «В кэше» в «Скачанном» — со срезом 4 | см. `git log --grep "воспроизведение"` |
| 3. Каталог | сделано | см. `git log --grep "каталог"` |
| 4. Библиотека | не начат | |
| 5. Аккаунт и синк (+ 0002) | ведёт основная сессия (свой worktree) | |
| 6. Тексты (+ 0001) | не начат | |
| 7. Отделка плеера | не начат | |
| 8. Выпуск (+ 0004, 0005, 0006) | не начат | |

### Срез 1. Каркас

Сделано:
- `project.yml` → `Melogold.xcodeproj`: таргет `Melogold` (iOS, macOS, visionOS — одно приложение), `Melogold Watch` (самостоятельное приложение watchOS, встроено в iOS), `MelogoldUITests`; версия в `Config/Version.xcconfig` (0.1.0, сборка 100), `scripts/set-version.sh` и `scripts/check-version.sh`.
- Пакет `MelogoldKit`: `MelogoldCore` (разделы, ключи настроек Android, `ServerAddressPolicy`, ссылки `melogold://`, журнал, MetricKit, версия), `MelogoldData` (папки `Application Support/Melogold` с `isExcludedFromBackup` у кэша и загрузок, настройки на `UserDefaults`), заготовки `MelogoldInnerTube`, `MelogoldServer`, `MelogoldPlayback`.
- Оболочка: iPhone и Vision — `TabView` с `Tab(role: .search)` (iOS 26: поле поиска внизу, в панели вкладок); iPad в широком окне и Mac — `NavigationSplitView` с одной `NavigationStack` в колонке детали; повторное нажатие на раздел — к корню; последний раздел при запуске; Mac — одно окно, ⌘1…⌘5, ⌘F, ⌘, и ⌘[.
- Настройки: «работает без аккаунта», «Сервер Melogold» (адрес по `ServerAddressPolicy`, «Незащищённое соединение» для http), тема, язык, версия.
- Ссылки `melogold://server` (экран «Сервер» с адресом) и `melogold://link` (инструкция; вход — срез 5).
- Иконки: `.icon` для iPhone, iPad, Mac и часов; набор visionOS (`AppIcon.solidimagestack`) из того же рисунка.
- Строки — `Shared/Resources/Localizable.xcstrings` (ru и en), `scripts/strings.py`; `PrivacyInfo.xcprivacy`, `ITSAppUsesNonExemptEncryption = NO`.
- CI: `.github/workflows/ci.yml` на образе `xcode-27`.

Проверено:
- `swift test`: 18 тестов, 37 векторов адреса сервера.
- Сборка: macOS, iOS Simulator (с часами), visionOS Simulator, watchOS Simulator.
- UI-тесты iPhone (6): повторное нажатие → корень, стеки разделов сохраняются, раздел восстанавливается после перезапуска, поле поиска внизу, ссылка `melogold://server` открывает «Сервер».
- Запуск: iPhone 18 Pro, iPad Pro 13 (боковая панель), Apple Watch Series 12 без iPhone, Apple Vision Pro (орнамент разделов), Mac (окно, колонка детали; боковая панель — только после разблокировки экрана).

Не сделано в срезе 1: снимок Mac с боковой панелью (экран Mac заблокирован); проверка меню Mac клавишами.

### Срез 2. Воспроизведение

Сделано:
- InnerTube на Swift (`MelogoldInnerTube`): клиенты WEB_REMIX и WEB, язык и регион системы, `visitorData`;
  поиск YouTube Music (сводка «Всё» с лучшим результатом, фильтры, продолжения), подсказки, поиск обычного YouTube
  (видео, каналы, трансляции, плейлисты, без Shorts), «Далее» (радио), `player`. Разбор — порт Windows, по фикстурам Android.
- Поток: `StreamResolver` (клиенты из `Config/stream-clients.json` с обновлением с `main`, LRU 64 до `expire − 5 мин`,
  два извлечения одновременно, сторож 20 с, классы ошибок REWRITE §4.10.3), `StreamSource` (кэш, googlevideo
  диапазонами, 403 → свежий адрес до двух раз, сеть — повторы через 1 и 3 с).
- **Плеер — свой движок на `AVSampleBufferAudioRenderer`** (бриф §4, грабли §9 п. 21–22): замеры показали, что AVPlayer
  у фрагментированного MP4 YouTube читает весь файл до звука (6–10 с) и удваивает длительность, а HLS через загрузчик
  роняет MediaToolbox. Свой разбор `moov`/`sidx`/`moof` → `CMSampleBuffer`; шкала синхронизатора с отрезками треков —
  переход без паузы; перемотка по фрагментам; ожидание данных без тишины; ошибки: повторы, пропуск, после трёх — стоп.
- Кэш музыки (задание 0003, `MelogoldData.AudioCache`): индекс в базе, файлы в `Application Support/Melogold/AudioCache`
  (`isExcludedFromBackup`), вытеснение по давности чтения, играющий и следующий защищены, смена формата стирает байты,
  сверка с папкой при старте; трек целиком в кэше играет без `player` и googlevideo; метка «Есть без сети»; без сети
  строки не из кэша приглушены, нажатие — «Нет сети»; «Настройки › Хранилище и данные»: размер (32 МБ … 8 ГБ, без
  ограничений, по умолчанию 4 ГБ), занято, «Очистить».
- Интерфейс: Поиск (недавние запросы, подсказки, области «Всё · Музыка · YouTube», фильтры, строки треков и видео,
  состояния и ошибки REWRITE §3.0), мини-плеер (`tabViewBottomAccessory` на iPhone, полоса на стекле на iPad, орнамент
  на Vision; смахивание — соседний трек; «Получаем поток…», «Долго…», причина ошибки), «Сейчас играет» (переход `zoom`,
  боком — обложка слева), панель воспроизведения Mac и меню «Управление» (пробел, ⌘→, ⌘←, ⌘↑, ⌘↓), системный
  «Сейчас играет» и пульт (`MPNowPlayingInfoCenter`, `MPRemoteCommandCenter`), AirPlay (`AVRoutePickerView`),
  аудиосессия `.playback` + `.longFormAudio`, звонки и отключение наушников.
- Часы: поиск (Песни, Видео), воспроизведение тем же движком без iPhone, системный `NowPlayingView`,
  без Bluetooth — «Подключите наушники или колонку».

Проверено:
- `swift test`: 40 тестов (парсеры на фикстурах Android ru и en, кэш, поток, синтетический фрагментированный MP4).
- Живые тесты: поиск, подсказки, VISIONOS → itag 140 → 206 у googlevideo; плеер играет через кэш, длительность 283,97 с,
  перемотка на 150 с.
- **Приёмка «от нажатия до звука» (Mac, пустой кэш, 10 разных треков): медиана 2,55 с, p90 4,18 с** — цель ≤ 3 с и ≤ 6 с.
- UI-тест iPhone: поиск → нажатие → «Пауза» (через отладочный прокси). Снимки: iPhone (выдача, мини-плеер),
  iPad (выдача, полоса мини-плеера), Mac (панель воспроизведения), часы (поиск, «Сейчас играет»).
- Сборки: macOS, iOS Simulator с часами, visionOS Simulator, watchOS Simulator.

Не сделано или не проверено:
- «Недавно игравшие» в корне Поиска — с историей (срез 4); «Вставить ссылку» — срез 3.
- На часах в симуляторе системный `NowPlayingView` показывает ▶, хотя движок играет: в симуляторе нет Bluetooth-вывода —
  проверить на настоящих часах.
- AirPlay, CarPlay и медиаклавиши проверяются на устройстве; экран Mac был заблокирован.

### Срез 3. Каталог

Сделано:
- InnerTube (`MelogoldInnerTube/Catalog.swift`, порт `YouTubeMusic.cs` Windows): «Обзор» (`ExplorePage` — полки по
  назначению, по ссылкам «Все», а не по заголовкам), настроения и настроение, новые релизы, альбом, плейлист с
  продолжениями старого и нового формата (`continuationItemRenderer`), исполнитель или канал (канал — если у YouTube
  Music нет «Популярного», альбомов и синглов, REWRITE §3.7.2), видео канала с продолжениями, `resolve_url` со вторым
  шагом для переадресации (`/@daftpunk` → `/daftpunk`), «Похожие».
- Ссылки: `YouTubeLinkParser` (MelogoldCore) по векторам `spec/youtube-links.vectors.json`; `AppModel.openLink` —
  видео (с `list=` — очередь плейлиста, `RD…` — радио, `t=` — позиция), плейлист (`OLAK5uy_` — альбом по первому
  треку), альбом, канал, `@handle` и `/c/`, `/user/` через `resolve_url`, поиск; Apple Music, Яндекс Музыка, Spotify —
  «Импорт из других сервисов появится позже»; прочее — «Эта ссылка пока не поддерживается».
  Входы: «Вставить» (`PasteButton`) в корне Поиска, ссылка в поле Поиска (подсказка «Открыть ссылку: …», Enter
  открывает цель), перетаскивание в окно (Mac, iPad), `onOpenURL`.
- Экраны: Тренды («В тренде» — сетка из 4 рядов вбок, «Весь список»; «Настроения и жанры» — плитки с полоской цвета
  API), Новое (новые альбомы и синглы — карусель; «Для вас» — карточка «Послушайте пару треков», подборка — со срезом 4),
  альбом, плейлист («Получено N», фоновая дозагрузка в очередь, пока очередь та же), исполнитель (одна прокрутка:
  «Популярное» с номерами, карусели, «Об исполнителе»), канал (видео 16:9, бесконечная прокрутка, «Перемешать видео»),
  настроение, «Все настроения», «Все новые релизы», полный список полки. Копия «Обзора» в `Caches` — без сети
  «Данные от 14:02».
- Детальные экраны: в узком окне шапка — первая строка списка, название переходит в панель навигации при прокрутке;
  в широком (iPad, Mac) — шапка слева, список справа.
- Строки в списках (`rowActions`, `contextMenu(forSelectionType:primaryAction:)`): iPhone, iPad, Vision — нажатие,
  долгое нажатие — меню, кнопка «…»; Mac — щелчок выделяет, двойной щелчок или Return играет, правый щелчок — меню.
  Так же переделана выдача Поиска.
- Меню трека (REWRITE §3.11.3, пункты без библиотеки): «Играть следующим», «В конец очереди», «Другие версии»,
  «Включить радио», «Открыть альбом», «Открыть исполнителя/канал», «Поделиться». Плашка над мини-плеером
  («Играет следующим: …») — общая с плашкой пропуска трека.
- Плеер: `playNext`, `enqueue` (перед блоком автовоспроизведения), `append(_:toQueue:)`, `playRadio`, старт с позиции;
  если следующий трек уже стоял на шкале, рендерер отбрасывает его отсчёты (`flush(fromSourceTime:)`) и подаётся
  новый следующий — переход без паузы сохраняется.
- Часы: Тренды и Новое — списки; альбом, плейлист, исполнитель, настроение, «Все настроения», «Все новые релизы» —
  обложка, «Слушать», «Перемешать», список треков; в Поиске — группы Альбомы, Исполнители, Плейлисты.

Проверено:
- `swift test`: парсеры каталога на фикстурах Android (ru и en), 79 векторов ссылок.
- UI-тесты iPhone и iPad (`CatalogUITests`, звук выключен): Тренды → «Весь список» → плейлист чарта, плитка →
  настроение; альбом → нажатие по строке → играет; ссылка в поле Поиска → подсказка → альбом; исполнитель и канал.
- Снимки (`apple-shots/s3/`): iPhone — Тренды, альбом, плейлист, исполнитель, канал, настроение, подсказка ссылки;
  iPad — Тренды, плейлист чарта, альбом с полосой мини-плеера; Mac (свой снимок окна) — Тренды, альбом в две
  колонки, исполнитель; часы — Тренды, Новое, альбом, исполнитель.
- Сборки: iOS Simulator с часами, macOS, visionOS Simulator, watchOS Simulator.

Не сделано или не проверено:
- «Для вас», «Похожие исполнители», «Похожие альбомы», «Плейлисты для вас» в Новом — нужна история (срез 4).
- «Сохранить», «Подписаться», «Скачать», «В вашей библиотеке» на страницах — библиотека (срез 4); кэш страниц
  альбома и плейлиста в базе (REWRITE §4.11.2) — там же.
- Mac: двойной щелчок и Return по строке проверены только сборкой — экран Mac был заблокирован. Свой снимок окна
  на Mac срабатывает через раз: при запусках подряд каждый второй не снимает и не выходит (приложение ждёт
  событий, окно, видимо, не показывается); причина не найдена — перезапуск помогает.
